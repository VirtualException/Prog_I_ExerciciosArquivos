#ifndef _3_ORDENAR_H
#define _3_ORDENAR_H

#include <stddef.h>

inline void switch_values(int* a, int* b);
int         list_order_onepass(int* list, size_t size);
void        list_order(int* list, size_t size);

#endif
